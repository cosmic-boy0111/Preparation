#include <bits/stdc++.h>

using namespace std;

// 1. array
// 2. 2d array
// 3. locally we can declare array of size max 1e5 or 10^5
int main() {

    // int n;
    // cin >> n;
    // int arr[n];
    // for(int i = 0; i < n; i++) {
    //     cin >> arr[i];
    // }
    // int sum = 0;
    // for (int i = 0; i < n; i++)
    // {
    //     sum += arr[i];
    // }

    // cout << sum << endl;

    // int n = 1e7;
    int n = 1e5;
    int a[n];
    a[n-1] = 7;
    cout << a[n-1] << endl;

}